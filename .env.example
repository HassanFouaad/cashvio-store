# =============================================================================
# Store-front Environment Configuration
# =============================================================================

# API Configuration
NEXT_PUBLIC_API_URL=http://localhost:3000/api/v1
NEXT_API_URL=http://localhost:3000/api/v1
NEXT_PUBLIC_API_TIMEOUT=30000

# Application Configuration
NEXT_PUBLIC_APP_NAME=Cashvio
NEXT_PUBLIC_APP_URL=https://cash-vio.com

# Note: Store-front uses store-specific cookies (locale, visitor ID)
# that are NOT cross-domain. Each store has its own settings.
# Auth for store-front users is handled separately if needed.

# Analytics (optional)
# NEXT_PUBLIC_GA_ID=
# NEXT_PUBLIC_FB_PIXEL_ID=

# Feature Flags
# NEXT_PUBLIC_ENABLE_ANALYTICS=false
# NEXT_PUBLIC_ENABLE_ERROR_REPORTING=false
